{
  "name": "oliverv",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && firebase deploy --only hosting",
    "tag-photos": "node scripts/tag-photos.mjs",
    "review-photos": "node scripts/review-photos.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "prompts": "^2.4.2",
    "sharp": "^0.33.0"
  }
}
